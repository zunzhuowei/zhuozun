#server:
#  config:
#    server:
#      host: 127.0.0.1
#      port: 7300
#    yml-name: zhuozun-discoverer-server-application
#    label: master
#    profile: dev
#
#spring:
#  cloud:
#      config:
#          uri: http://${server.config.server.host}:${server.config.server.port}
#          name: ${server.config.yml-name}
#          label: ${server.config.label}
#          profile: ${server.config.profile}

# 引用监控限制内容Map <Key String>
info:
  build.artifactId: "@project.artifactId@"
  build.version: "@project.version@"
  spring-cloud-version: "@spring.cloud.verion@"